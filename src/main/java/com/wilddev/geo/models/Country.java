package com.wilddev.geo.models;

public record Country(String code, String name) {

    @Override
    public String toString() {
        return "Country{" +
                "code='" + code + '\'' +
                ", name='" + name + '\'' +
                '}';
    }
}
